SELECT id, name FROM CUSTOMER
ORDER BY name desc, id asc;

--Use CUSTOMER table created from Customer Credit Limit problem
--Write a query to print the IDs and the NAMEs of the customers sorted by
--NAME in descending order
--if two or more customers have the same NAME, then sort by ID in ascending order

INSERT INTO CUSTOMER VALUES (14, 'Anne Harris', 'Canada', 600000);